version: 0.2
phases:
  build:
    commands:
      - echo Build started 
      - echo Compiling the Python code…..
      - pip install requests -t .
  post_build: 
    commands:
      - echo Build completed 
      - pwd
      - Is -al
  
  artifacts:
    files:
    - '**/*'
